apiVersion: v1
kind: Service
metadata:
  name: store-admin-rb-ws-service
spec:
  selector:
    app: store-admin-rb-ws
  ports:
    - protocol: TCP
      port: 3040
      targetPort: 3040
  type: NodePort
  
